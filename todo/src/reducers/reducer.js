const initialState = {
    item: 'Learn about reducers',
    completed: false,
    id: Date.now
}

const reducer = (state, action) => {
    switch (action.type) {
        default:
            return state;
    }
}